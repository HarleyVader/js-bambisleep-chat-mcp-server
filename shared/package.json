{
    "name": "@bambisleep/mcp-shared",
    "version": "1.0.0",
    "description": "Shared utilities and types for MCP Server & Agent",
    "main": "index.js",
    "scripts": {
        "build": "echo 'Building shared utilities...' && npm run test:optional",
        "clean": "rimraf dist",
        "test": "jest",
        "test:optional": "jest || echo 'Tests skipped - no test files found'",
        "lint": "eslint src/ --ext .js",
        "lint:fix": "eslint src/ --ext .js --fix",
        "lint:optional": "eslint src/ --ext .js || echo 'Linting skipped due to errors'"
    },
    "dependencies": {},
    "devDependencies": {
        "jest": "^29.7.0",
        "eslint": "^8.57.0",
        "rimraf": "^5.0.5"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
